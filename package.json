{
  "name": "fit-visualizer",
  "displayName": "FIT Visualizer",
  "version": "0.11.0",
  "description": "Visualize cycling and running FIT files and get AI-assisted training analysis",
  "publisher": "AntonPetrusevich",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "icon": "icon.png",
  "main": "./extension.js",
  "contributes": {
    "customEditors": [
      {
        "viewType": "fitVisualizer.fitEditor",
        "displayName": "FIT Visualizer",
        "selector": [
          {
            "filenamePattern": "*.fit"
          },
          {
            "filenamePattern": "*.FIT"
          }
        ],
        "priority": "default"
      }
    ],
    "commands": [
      {
        "command": "fitVisualizer.openFit",
        "title": "FIT: Visualize File"
      },
      {
        "command": "fitVisualizer.browse",
        "title": "FIT: Browse Loaded Data"
      },
      {
        "command": "fitVisualizer.indexAll",
        "title": "FIT: Index All Files"
      },
      {
        "command": "fitVisualizer.indexNew",
        "title": "FIT: Index New Files"
      },
      {
        "command": "fitVisualizer.indexOne",
        "title": "FIT: Index This File"
      },
      {
        "command": "fitVisualizer.addManualActivity",
        "title": "FIT: Add Manual Activity"
      },
      {
        "command": "fitVisualizer.reanalyzeOutdated",
        "title": "FIT: Re-analyze Outdated Analyses"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "when": "resourceExtname == .fit",
          "command": "fitVisualizer.openFit",
          "group": "navigation"
        },
        {
          "when": "resourceExtname == .fit",
          "command": "fitVisualizer.indexOne",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "FIT Visualizer",
      "properties": {
        "fitVisualizer.maxHeartRate": {
          "type": [
            "number",
            "null"
          ],
          "default": null,
          "minimum": 100,
          "maximum": 240,
          "description": "Legacy fallback maximum heart rate. Dated profiles entered in the activity view take precedence."
        },
        "fitVisualizer.logLlmRequests": {
          "type": "boolean",
          "default": true,
          "description": "Write each Copilot prompt and response to .fit-visualizer/logs next to the database."
        },
        "fitVisualizer.llmLogRetentionDays": {
          "type": "number",
          "default": 30,
          "minimum": 0,
          "description": "Delete Copilot request logs older than this many days. Set to 0 to keep them forever."
        },
        "fitVisualizer.lmVendor": {
          "type": "string",
          "default": "copilot",
          "description": "VS Code language-model vendor ID used for activity analysis and chat."
        },
        "fitVisualizer.preferCheapAnalysisModel": {
          "type": "boolean",
          "default": false,
          "description": "For one-off activity analysis (not chat), prefer a cheaper/smaller language model over whichever model the Copilot picker currently uses. Tries an Auto model family first, then a model-name heuristic, then falls back to the default model. Neither path is officially guaranteed by the VS Code API."
        },
        "fitVisualizer.cheapModelMarkers": {
          "type": "array",
          "items": { "type": "string" },
          "default": ["haiku", "mini", "flash", "nano", "lite", "small"],
          "description": "Model name substrings (case-insensitive) used to pick a cheaper model when fitVisualizer.preferCheapAnalysisModel is enabled and no Auto model family is available."
        },
        "fitVisualizer.segmentation.gradeThresholdPct": {
          "type": "number",
          "default": 2.5,
          "minimum": 0.5,
          "maximum": 15,
          "description": "Grade in percent that separates climbs and descents from flat terrain."
        },
        "fitVisualizer.segmentation.gradeHysteresisPct": {
          "type": "number",
          "default": 0.5,
          "minimum": 0,
          "maximum": 5,
          "description": "Extra grade margin required to switch terrain type, preventing flapping around the threshold."
        },
        "fitVisualizer.segmentation.minSegmentSeconds": {
          "type": "number",
          "default": 45,
          "minimum": 5,
          "maximum": 600,
          "description": "Shorter segments are merged into a neighbour."
        },
        "fitVisualizer.segmentation.technicalGradePct": {
          "type": "number",
          "default": -8,
          "minimum": -30,
          "maximum": 0,
          "description": "Descent grade below which an erratic speed trace marks the segment as technical (no effort estimate)."
        },
        "fitVisualizer.segmentation.effortWindowSeconds": {
          "type": "number",
          "default": 10,
          "minimum": 1,
          "maximum": 120,
          "description": "Averaging window for the effort signal before splitting a segment into intervals."
        },
        "fitVisualizer.segmentation.minEffortMacroSeconds": {
          "type": "number",
          "default": 600,
          "minimum": 60,
          "maximum": 3600,
          "description": "Minimum terrain-segment duration before splitting it into separate effort intervals."
        },
        "fitVisualizer.segmentation.effortMergeTolerancePct": {
          "type": "number",
          "default": 12,
          "minimum": 1,
          "maximum": 50,
          "description": "Maximum adjacent effort difference in percent to merge into one continuous terrain segment."
        },
        "fitVisualizer.segmentation.effortCostThreshold": {
          "type": [
            "number",
            "null"
          ],
          "default": null,
          "description": "Merge cost limit for interval detection. Leave empty to derive it from the noise of the effort signal."
        },
        "fitVisualizer.segmentation.stopSpeedKmh": {
          "type": "number",
          "default": 1,
          "minimum": 0,
          "maximum": 10,
          "description": "Speed at or below which a record counts as stopped."
        },
        "fitVisualizer.segmentation.stopMinSeconds": {
          "type": "number",
          "default": 10,
          "minimum": 3,
          "maximum": 300,
          "description": "Minimum duration of a stop, and the recording gap that counts as an auto-paused stop."
        },
        "fitVisualizer.segmentation.gpsTrustMinKm": {
          "type": "number",
          "default": 1,
          "minimum": 0.2,
          "maximum": 10,
          "description": "Minimum continuous distance before a GPS window can confirm the recorded speed."
        }
      }
    }
  },
  "scripts": {
    "check": "node --check extension.js && node --check activity-summary.js && node --check activity-webview.js && node --check analysis.js && node --check chart-data.js && node --check chart-geometry.js && node --check chart-overlays.js && node --check commands.js && node --check database-schema.js && node --check dynamic-localization.js && node --check fit-files.js && node --check heart-rate.js && node --check ui-strings.js && node --check utils.js && node --test test/core.test.js",
    "lint": "npm run check",
    "package": "vsce package"
  },
  "keywords": [
    "fit",
    "garmin",
    "cycling",
    "running",
    "visualization",
    "wahoo",
    "polar",
    "coros",
    "cycplus",
    "copilot"
  ],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jef-sure/fit-visualizer.git"
  },
  "type": "commonjs",
  "dependencies": {
    "fit-file-parser": "^4.1.0",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@types/vscode": "^1.95.0",
    "@vscode/vsce": "^3.9.2"
  }
}
